/** Trading Range drawer: poll engine snapshots + range events while chart layers expect fresh data (ms). */
export const TRADING_RANGE_DRAWER_REFRESH_MS = 20_000;

/** Signal dashboard drawer: poll engine snapshots when auto-refresh is on (ms). */
export const SIGNAL_DASHBOARD_DRAWER_REFRESH_MS = 20_000;
